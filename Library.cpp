
// Link Librarys
    //A file contating procducers that have been compiled into machine code 
    // similar to header files
    
// Calling a library procucure 
//      call WriteHex
//      call Crif 
//      call WriteInHex (Rewrites a decimal number into hex or binary to hex)

// Linking to a Library 
    //make32.bat is part of Microsoft Win32 Software development kit
    // When Call something in a library filed it links to .lib
            //Can link directly to something else as well then executes

// Library Procedures
    //CloseFile - Closes an open disk file 
    //


    //MsgBox (Can be done in C++) - Display popup msg box
    //Randomize - seeds the random number generator 
    //Str_compare - compare two strings 
    //Str_copy
    //Str_length


// Stack Operations
    //Runtime stack
        //First in Last out
        // 2 Registers SS (Stack Segment) / SP (Stack Pointer)
    //PUSH operations
        //Contents doesnt move but the pointer does
        // push esi
        // push ecx
    //POP operations
        // pop ebx 
        // pop ecx

// Creating Procedures 
    //Large problems can be divided into smaller tasks to make them more manageable 

// Documenting procedires 
    // should have code & comments for what it does 
    // what it returns 
    // what function requires 
    // EX
        // : faiwnff eawe
        // : sigrnwa
        // : overflow, etc
        // : Requires : nothing

// CALL and RET Instructions
    // Call calls a precdure 
        //pushes offset of next instruction on the stack 

// Can nest things
    // jmp L2    (Jump to loop2)

    // L2:
    //       jump L1 (Command to jump to loop1) 

// Procedure parameters 
    //Flexable as possible 
    //Like unity code should be able to do multiple things (Not Hard Coded)

// Flowchart symbols 
    // Oval 
        // Begin / end
    // Rectanble
        //process
    //Slope 
        //manual input
    //Bullet 
        //display
    //Diamond
        //decision (Yes / No (else ifs))

// Program Designs
    //Top-Down
        //Looking at goal then see what you need to get to that
    //Down-up
        //

// 