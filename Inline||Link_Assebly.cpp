// Adds Assebly language into other code languages 
    // Both must have the same naming convenctions
    // Same calling conventions

// Calling conventions
    // Determines how arguments (Parameters) are passed in procedure 

    __asm {
        //Assembly code
    }

