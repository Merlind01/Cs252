// Conditinal Statements

    // IF
        // Use jne L1 (Jump to L1 if not equal) or jmp L2 (Just Jump to L2)
        // L1 : mov x,2
        // L2

// Compound Statements with And
    // if (al > bl) AND (bl > cl)
    //   x = 1;

    //Ways to write this 
    // cmp al, bl        // first expression
    // jbe next          // Quit if false
    // cmp bl, cl        // second expression
    // jbe next          // quit if false
    // mov x, 1          // If both true then do this

// Compound Statements with Or
    // if (al > bl) OR (bl > cl)
    //   x = 1;

    // Ways to write this
    //

// Compound Statements with While


    //

// Finite-State Machine
    // Accepts any sequence of symbols that puts it into an accepting (final) State
    // Visual Aid ***
        // Like a flow chart

// 